from .views import *
from django.urls import path

urlpatterns = [
    # path('',home, name = 'home'),
    # path('adds',add, name = 'adds'),
    # path('edits/<int:id>/',edit, name = 'edits'),
    # path('updates/<int:id>/', update, name='updates'),
    # path('deletes/<int:id>/', delete, name="deletes"),

]